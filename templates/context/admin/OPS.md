# Ops (ADMIN TIER — references only, NEVER secret values)

> Env layout, infra/deploy detail, and the *names/locations* of secrets only.
> A real secret value here will be blocked by the gitleaks CI gate.

## Environments
## Infra / deploy
## Secret references (name → where it lives)
