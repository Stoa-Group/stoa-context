# CLAUDE.md — stoa-context

> Stoa Group repo. Org context: `Stoa-Group/stoa-skills` (CLAUDE.md + wiki/).

---
## ⛔ HARD RULE — org wiki: consult BEFORE, update AFTER (locked 2026-07-06, Alec directive)

**Corrected 2026-09-21 — this was a full duplicate of the canonical rule (the design
intent was always a short "echo block", per the canonical file's own "Reach/enforcement"
note) and had drifted stale independently (still said "memoirs = training data" after
that was corrected elsewhere). Canonical text now lives in ONE place —
`Stoa-Group/stoa-skills` → `CLAUDE.md` §"⛔ HARD RULE: the org wiki is consulted BEFORE
and updated AFTER every workflow" — read it there. Do not re-copy the rule text here
again; link to it instead so it can't drift out of sync a second time.**
